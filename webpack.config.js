const Webpack = require('webpack');
// const HtmlWebpackConfig =
// const ClearWebpackPlugin
const path = require('path');
const resolve = (dir) => path.join(__dirname, dir);

module.exports = {
  devtools: '',
  mode: 'development',
  devServer: {},
  plugins: [],
};
